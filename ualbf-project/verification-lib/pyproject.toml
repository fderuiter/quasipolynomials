[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "verification-lib"
version = "0.1.0"
description = "Native validation bridge for UALBF certificates"
requires-python = ">=3.7"

[tool.maturin]
features = ["python"]
module-name = "verification_lib"
